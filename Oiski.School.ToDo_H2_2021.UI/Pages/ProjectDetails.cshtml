@page  "{id}"
@model Oiski.School.ToDo_H2_2021.UI.Pages.ProjectDetailsModel
@{
    ViewData[ "Title" ] = Model.Project.Name;
}


<h1 class="text-center">@Model.Project.Name</h1>

<div class="row">
    <div class="col-md-2">
        <p>ID: </p>
    </div>
    <div class="col-md-10">
        <input readonly="readonly" type="text" asp-for="Project.ID" />
    </div>
</div>
<div class="row">
    <div class="col-md-2">
        <p>Name: </p>
    </div>
    <div class="col-md-10">
        <input readonly="readonly" type="text" asp-for="Project.Name" />
    </div>
</div>
<div class="row">
    <div class="col-md-2">
        <p>Description: </p>
    </div>
    <div class="col-md-10">
        <textarea rows="10" cols="50" readonly="readonly" asp-for="Project.Description"></textarea>
    </div>
</div>
<div class="row">
    <div class="col-md-2">
        <p>Status: </p>
    </div>
    <div class="col-md-10">
        <input readonly="readonly" type="text" asp-for="Project.Status" />
    </div>
</div>

<h2 class="text-center">Tasks</h2>
<p>
    <a asp-page="CreateTask" asp-route-id="@Model.Project.ID">New Task</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                To Do
            </th>
            <th>
                Description
            </th>
            <th>
                Status
            </th>
            <th>
                Settings
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach ( var item in Model.Project.GetEnumerable ().ToList () )
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <i>@Html.DisplayFor(modelItem => item.Description)</i>
                </td>
                <td>
                    <i>@Html.DisplayFor(modelItem => item.Status)</i>
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>